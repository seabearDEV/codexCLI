{
  "_meta": {
    "arch.audit": 1775497218448,
    "arch.binary": 1775347252160,
    "arch.cli": 1775530717052,
    "arch.interpolation": 1775530712709,
    "arch.lint": 1775347259273,
    "arch.llmInstructions": 1775358827605,
    "arch.mcp": 1775497222452,
    "arch.missPathTracking": 1775518365836,
    "arch.projectResolution": 1775530704879,
    "arch.scaffold": 1775508051571,
    "arch.scope": 1775347247705,
    "arch.staleness": 1775347257904,
    "arch.storage": 1775347244612,
    "arch.store": 1775358827551,
    "arch.storeLayout": 1775609487958,
    "arch.telemetry": 1775500435763,
    "arch.tokenSavings": 1775517276576,
    "commands.build": 1775526064911,
    "commands.check": 1775353132791,
    "commands.clean": 1775353133317,
    "commands.lint": 1775526065350,
    "commands.release": 1775362842974,
    "commands.test": 1775526064504,
    "context.aliasCollision": 1775356765156,
    "context.configSeparate": 1775347273418,
    "context.confirmFlow": 1775493255368,
    "context.devWorkflow": 1775358827618,
    "context.execInterpolation": 1775362844353,
    "context.mcpCwd": 1775530709022,
    "context.migration": 1775347272043,
    "context.shortFlagAudit": 1775530720179,
    "context.testDataLeaks": 1775360216518,
    "conventions.atomicWrites": 1775347269392,
    "conventions.commits": 1775526068051,
    "conventions.debug": 1775347270745,
    "conventions.editSurface": 1775609186846,
    "conventions.output": 1775347266674,
    "conventions.persistence": 1775359536319,
    "conventions.tests": 1775347268052,
    "conventions.types": 1775347265850,
    "deps.chalk": 1775361267185,
    "deps.commander": 1775361265842,
    "deps.esbuild": 1775361268959,
    "deps.mcp-sdk": 1775361264498,
    "deps.vitest": 1775361270072,
    "deps.zod": 1775361268065,
    "files.audit": 1775356700889,
    "files.completions": 1775356698632,
    "files.config": 1775356704687,
    "files.dataManagement": 1775356696458,
    "files.entries": 1775356695395,
    "files.entry": 1775356690533,
    "files.formatting": 1775356697352,
    "files.interpolate": 1775356699445,
    "files.llmInstructions": 1775356703142,
    "files.mcp": 1775501385271,
    "files.storage": 1775356693993,
    "files.store": 1775356692683,
    "files.telemetry": 1775356701809,
    "files.types": 1775356704269,
    "project.description": 1775347231858,
    "project.goals": 1775361263177,
    "project.name": 1775347230199,
    "project.repo": 1775526066241,
    "project.tracking": 1775526069499,
    "project.vision": 1775501535503
  },
  "aliases": {
    "chk": "commands.check",
    "cln": "commands.clean",
    "dev": "context.devWorkflow",
    "goals": "project.goals",
    "mcp": "arch.mcp",
    "persist": "conventions.persistence",
    "rel": "commands.release",
    "store": "arch.store",
    "tests": "conventions.tests"
  },
  "confirm": {
    "commands.release": true
  },
  "entries": {
    "commands": {
      "release": "git push origin main && VERSION=`node -p \"require('./package.json').version\"` && git tag \"v$VERSION\" && git push origin \"v$VERSION\" && echo \"🚀 Released v$VERSION\"",
      "check": "npm run build && npm run lint && npm test",
      "clean": "rm -rf dist && npm run build",
      "test": "npm test",
      "build": "npm run build",
      "lint": "npm run lint"
    },
    "project": {
      "name": "codexcli",
      "description": "Persistent, structured knowledge store for software projects. CLI + MCP server. Agents bootstrap full project context in one call instead of re-exploring the codebase.",
      "vision": "Eliminate repetitive codebase exploration by AI agents. Every session starts with full context. Every insight gets stored for the next session. As the knowledge base grows, hit rates improve and agents spend fewer tokens exploring — tracked via per-period trends in codex_stats.",
      "goals": "1) Token efficiency — fewer tokens per session. 2) Dogfooding — use codexCLI MCP for own project maintenance. 3) Dual-audience .codexcli.json — useful for devs AND GenAI agents. 4) Agent-agnostic — work across Claude, Copilot, ChatGPT, Cursor, etc.",
      "repo": "https://github.com/seabearDEV/codexCLI",
      "tracking": "GitHub Issues is the source of truth for planned work. ROADMAP.md is a vision doc with issue links, not a task tracker. CHANGELOG.md is the release record."
    },
    "arch": {
      "storage": "Unified data.json: { entries, aliases, confirm, _meta }. Project-scoped at .codexcli.json, global at ~/.codexcli/data.json.",
      "store": "ScopedStore with mtime caching, auto-migration from old format, atomic writes (tmp file + rename with file locking).",
      "scope": "Three scopes: project, global, auto. Auto uses project if .codexcli.json exists. Single-key lookups fall through project -> global.",
      "mcp": "19 tools via @modelcontextprotocol/sdk. All tools accept optional scope param. Telemetry + audit auto-logged per call.",
      "cli": "Commander.js CLI. Core CRUD: set/s, get/g, run/r, find/f, edit/e, copy/cp, rename/rn, remove/rm. Subcommand groups: alias (set/remove/list/rename), confirm (set/remove/list), config, data. Top-level: context, info, stale, lint, stats, audit. Hidden: search (alias for find). Deprecated (still functional, hidden): -a flags on set/get/remove/rename (use alias subcommand instead); --raw on get and context (renamed to --plain in v1.9.1).",
      "binary": "Production: ccli (Node.js SEA binary). Dev: cclid (npm-linked). getBinaryName() auto-detects — never hardcode.",
      "interpolation": "Supports value refs (dollar-brace syntax), exec refs (dollar-paren syntax), fallback defaults (:-), and required checks (:?). Backslash-escape emits literals (backslash consumed on output). Recursive with circular-ref detection. --source flag skips interpolation on get; --plain only strips colors (--raw is a deprecated alias for --plain).",
      "telemetry": "JSONL append at ~/.codexcli/telemetry.jsonl. Tracks tool, session, op type, namespace, source (mcp/cli), scope, agent (CODEX_AGENT_NAME). Enriched with duration, hit/miss, redundant, responseSize. codex_stats computes: delivery floor (bytes/4), exploration estimate (namespace-weighted multipliers for tool calls avoided), redundant write savings, per-namespace breakdown, trending metrics, and per-agent breakdowns. EXPLORATION_COST map exported from telemetry.ts for transparency.",
      "staleness": "_meta timestamps per key, auto-updated on write. ccli stale / codex_stale surfaces old entries. codex_context shows age tags >30d.",
      "lint": "Validates entries against recommended namespace schema. Custom namespaces via _schema.namespaces in .codexcli.json.",
      "llmInstructions": "Built-in defaults + customizable via system.llm.instructions entry. ccli config llm-instructions to view/edit.",
      "scaffold": "ccli init scans codebase via scanCodebase() in src/commands/scan.ts — 6 composable detectors (detectProject, detectCommands, detectFiles, detectDeps, detectConventions, detectContext). Generates CLAUDE.md via src/commands/claude-md.ts. Seeds conventions.persistence entry for three-file balance. Flags: --no-scan, --no-claude, --force, --dry-run. Idempotent — skips existing keys.",
      "audit": "JSONL at ~/.codexcli/audit.jsonl. Captures before/after values, success/fail, scope, agent identity, enriched metrics (duration, responseSize, hit/miss, redundant, entryCount). CLI reads (get, search, stale) also logged. codex_audit (MCP) + ccli audit (CLI) to query with --detailed. Encrypted values masked, passwords redacted. CLI uses sync writes to guarantee log durability on process exit.",
      "tokenSavings": "Observed miss-path costing replaces static EXPLORATION_COST multipliers. Three layers: (1) miss-path windows track exploration cost when codex_get misses, (2) calibrated cost map uses median of observed miss-paths per namespace with static fallback when <5 samples, (3) net savings = gross exploration avoided − delivery cost (responseSize of all read hits). MissPath struct: { ts, session, namespace, key, toolCalls, explorationBytes, resolution }. Resolution types: writeback (clean), moved_on, timeout. Bootstrap savings use entry-count proxy with calibrated per-namespace costs.",
      "missPathTracking": "MissWindowTracker in telemetry.ts: pure state machine tracking exploration cost when codex_get misses. Opens window on hit:false, closes on writeback (codex_set to same ns), moved_on (any hit), or timeout (5min). Persists to miss-paths.jsonl. MCP server wires tracker in wrapper finally block + flushes on beforeExit. getExplorationCost() returns observed median (>=5 writeback samples) or falls back to static EXPLORATION_COST map. computeStats() reports: deliveryCostTokens (bytes served / 4), netTokensSaved (gross - delivery), calibration map (observed vs static per namespace).",
      "projectResolution": "findProjectFile() resolution order (src/utils/paths.ts): 1) CODEX_NO_PROJECT env → disabled, 2) CODEX_PROJECT env → explicit .codexcli.json file or containing dir; fails closed if missing (no cwd fallback), 3) setProjectRootOverride() programmatic override (set by MCP server from client roots and from CODEX_PROJECT_DIR/--cwd launcher hints), 4) walk up from process.cwd(). MCP server captures roots via listRoots() after connect; falls back silently if client doesn't support roots. Result is cached; clearProjectFileCache() resets.",
      "storeLayout": "Planned for v1.10.0 (tracked in #54): file-per-entry layout. Each entry lives in .codexcli/<namespace>.<name>.json with shape {value, meta:{updated, ...}}. Sidecars: .codexcli/_aliases.json, .codexcli/_confirm.json. Replaces single .codexcli.json (auto-migrated, old file renamed to .backup). In-memory shape returned by store.ts is preserved — consumers above the store layer don't change. Rationale: eliminates _meta sibling-key fragility (rename desync), reduces multi-dev merge conflicts to only same-key concurrent edits, and meta becomes per-entry-extensible without schema migrations. Hand-editing files unsupported (see conventions.editSurface)."
    },
    "conventions": {
      "types": "tsconfig has exactOptionalPropertyTypes: true — optional interface fields need | undefined",
      "output": "All user-facing text uses getBinaryName() — never hardcode ccli",
      "tests": "Vitest with globals. Unit tests mock fs/store. Integration tests use temp dirs via CODEX_DATA_DIR.",
      "atomicWrites": "All file writes use saveJsonSorted -> withFileLock -> atomicWriteFileSync (tmp file + rename)",
      "debug": "Always use process.env.DEBUG === 'true' (strict equality). Never use truthy check.",
      "persistence": ".codexcli.json = project knowledge (any agent needs it). CLAUDE.md = Claude behavior directives (how to use codex, not what it contains). MEMORY.md = personal user preferences only (commit style, interaction prefs). Rule: if another agent would benefit, it goes in .codexcli.json.",
      "commits": "Conventional commits: fix:, feat:, docs:, test:, refactor:. Version tags (v1.9.0) on release commits. Keep subject under 70 chars, use body for detail.",
      "editSurface": "Supported edit paths for codex data: ccli (CLI), MCP tools, future UI. Direct editing of .codexcli.json or .codexcli/*.json is unsupported — you will desync metadata (timestamps, future verified/agent fields) and break staleness signals. The wrapper format {value, meta} assumes only the official tools touch it; raw edits skip the meta updates the tools handle automatically."
    },
    "context": {
      "migration": "Auto-migrates old 3-file format to unified data.json on first access. Old files renamed to .backup.",
      "configSeparate": "config.json is separate from data.json — user preferences, not project data",
      "mcpCwd": "MCP server resolves the project file via arch.projectResolution (CODEX_PROJECT env → MCP client roots → cwd walk-up). Old CODEX_PROJECT_DIR and --cwd launcher hints still work but now apply via setProjectRootOverride, not process.chdir, and work whether the server is run as a binary or imported. Pin deterministically by setting \"env\": { \"CODEX_PROJECT\": \"<repo path>\" } in the codexcli MCP block of .claude.json.",
      "testDataLeaks": "CODEX_DATA_DIR redirects global store + audit/telemetry logs to temp dir during tests. Project store (.codexcli.json) can still get test entries if it exists in cwd — always clean before committing.",
      "devWorkflow": "Use cclid (dev) / cclid-mcp (MCP dev) for local testing — never ccli in dev. Release: git tag triggers CI.",
      "aliasCollision": "Alias always wins in resolveKey — no way to bypass and access an entry with the same name as an alias. Known limitation.",
      "execInterpolation": "Shell $(...) collides with codexCLI's exec interpolation syntax $(key). In stored commands, use backticks instead: `cmd` not $(cmd).",
      "confirmFlow": "MCP codex_run uses two-step confirmation for --confirm entries: step 1 returns command preview + one-time confirm_token, step 2 passes token back to execute. Token is single-use, key-scoped, 5min TTL. force:true and dry:true bypass confirm (CLI fallback).",
      "shortFlagAudit": "Short-flag space is inconsistent across commands and needs a holistic audit in an upcoming release. Symptom that triggered this: --plain shipped without a short flag in v1.9.1 because -p was already taken by --preview elsewhere, and -P felt awkward as a one-off. Don't add new short flags piecemeal — wait for the audit so the whole namespace gets reorganized at once."
    },
    "files": {
      "entry": "src/index.ts — CLI entry point, Commander.js setup, all command registration, CLI-side audit logging",
      "mcp": "src/mcp-server.ts — MCP server, 19 tools, audit/telemetry wrapper, scope resolution",
      "store": "src/store.ts — ScopedStore class, mtime caching, auto-migration, meta timestamps, atomic writes via file locking",
      "storage": "src/storage.ts — low-level getValue/setValue/getEntriesFlat, scope fallthrough (project->global), alias resolution",
      "entries": "src/commands/entries.ts — get/set/run/remove command handlers, interpolation integration, clipboard support",
      "dataManagement": "src/commands/data-management.ts — copy/rename/import/export/reset handlers",
      "formatting": "src/formatting.ts — display formatting for get output, tree/flat views, color, truncation",
      "completions": "src/completions.ts — zsh tab completion, CLI_TREE definition, generateZshScript(), arg type handlers",
      "interpolate": "src/utils/interpolate.ts — ${key} value refs, $(key) exec refs, defaults, required checks, circular detection",
      "audit": "src/utils/audit.ts — logAudit, loadAuditLog, queryAuditLog, sanitizeValue/Params, JSONL append",
      "telemetry": "src/utils/telemetry.ts — logToolCall, computeStats, JSONL telemetry at ~/.codexcli/telemetry.jsonl",
      "llmInstructions": "src/llm-instructions.ts — DEFAULT_LLM_INSTRUCTIONS, getCustomInstructions(), getEffectiveInstructions()",
      "types": "src/types.ts — shared interfaces: DataStore, Scope, AliasMap, ConfirmMap, StoreMeta",
      "config": "src/config.ts — user config (config.json), separate from data store, preferences like pager/color"
    },
    "deps": {
      "mcp-sdk": "@modelcontextprotocol/sdk — MCP protocol implementation. Provides Server class, tool registration, stdio transport. Pinned to ^1.26.0.",
      "commander": "commander ^13 — CLI framework. Handles arg parsing, subcommands, help generation. Every command has a short alias registered via Commander.",
      "chalk": "chalk ^4 (CJS) — terminal colors. Pinned to v4 for CommonJS compatibility; v5+ is ESM-only.",
      "zod": "zod ^4 — runtime schema validation. Used for MCP tool input validation and config parsing.",
      "esbuild": "esbuild ^0.25 — bundler for SEA builds. Produces single-file bundle that gets injected into Node binary via postject.",
      "vitest": "vitest ^4 — test runner. Globals enabled, coverage via @vitest/coverage-v8. CODEX_DATA_DIR isolates test data."
    }
  }
}